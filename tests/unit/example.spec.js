import { expect } from 'chai'
import { shallowMount } from '@vue/test-utils'
import Button from '../../src/Button/Button'

describe('Button', () => {
  it('Button存在', () => {
    expect(1).to.eq(1)
  })
})
